const Discord = require("discord.js");
module.exports.run = async (bot, message, args) => {
		let icon = sender.displayAvatarURL;
			message.channel.send(icon);

}


module.exports.help = {
	name: "profilepicture"
}
