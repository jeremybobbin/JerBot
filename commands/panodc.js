const Discord = require("discord.js");
module.exports.run = async (bot, message, args) => {
			if(args < 1 || args2 < 1){
							message.channel.send(`ywouw nweed 2 parmaters two cwonvwert fwiles *giggles*`);	
						}	
			//var dest = "../downloads/"
	//		//var download = function(args, dest, cb)
}


module.exports.help = {
	name: "pandoc"
}
