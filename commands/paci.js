const Discord = require("discord.js");
module.exports.run = async (bot, message, args) => {
			if(!message.mentions.users.size) {
							message.channel.send(`pops paci into ${users} mouth`)
						sage.channel.send(`${user} pops a paci into ${TaggedUser} mouth`);
					}
}


module.exports.help = {
	name: "paci"
}
