const Discord = require("discord.js");
module.exports.run = async (bot, message, args) => {
			if(!message.mentions.users.size) {
							return message.channel.send(`nini ${users}`)
						}

			message.channel.send(`nini ${TaggedUser}`);
		}




module.exports.help = {
	name: "nini"
}
